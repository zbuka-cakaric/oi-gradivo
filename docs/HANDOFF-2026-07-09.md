# HANDOFF — stanje projekta 2026-07-09 (v129 → **v183** + DGU saga + ATOM odluka)

> Za novi razgovor: pročitaj OVO + povuci bibliju s raw.githubusercontent.com/zbuka-cakaric/oi-gradivo/main/docs/ (00 + fazno-relevantne; za Investitor: 10 + OI-DGU-IZVORI-PODATAKA + OI-Investitor-MASTER-strategija). Kod = istina.
> **PRETHODNI HANDOFF:** HANDOFF-2026-07-07.md (v122→v128). Ovaj pokriva **v129→v183**.

## 1. PROJEKT U JEDNOJ REČENICI
**ŽBUKA AI** (oi-ispit.zbuka.hr) — tri moda nad istom bazom hrvatskih propisa i istim AI mozgom: **Mentor** (plava, ispit: pismeni + usmeni AI ispitivač), **Vještak** (narančasta, pravni pomoćnik s citatima i privicima), **Investitor** (zelena, od kčbr do investicijske studije — **F1 u produkciji**).

## 2. INFRASTRUKTURA (konstante — nepromijenjeno + novo)
- Railway (Node 22/Express + PG: pgvector, pg_trgm, unaccent) · repo **zbuka-cakaric/oi-ispit** (server.js + index.html + sw.js + test-ui-v043.js + **dopis-docx.js + dopis-pdf.js** — 6 datoteka u svakoj isporuci) · docs u **zbuka-cakaric/oi-gradivo**
- init-db: `/api/init-db?key=io-ispit-2026` ("io"!) — samo uz promjenu sheme
- ENV: JWT_SECRET, PWD_PEPPER, ANTHROPIC_API_KEY, VOYAGE_API_KEY, MODEL_ODGOVOR=claude-sonnet-5, MODEL_PLANNER=claude-haiku-4-5-20251001, GOOGLE_TTS_KEY, RESEND_FROM, APP_URL, SUPERADMIN_EMAIL=info@zbuka.hr, AI_CIJENA_IN=3/OUT=15, TOKENI_PO_USD=1e6; **NOVO:** DGU_TOKEN (Ivanov ključ — pričuva, tajna), DGU_WFS_URL (NE postavljati), DGU_TOKEN_PARAM (def 'token')
- Superadmin info@zbuka.hr; Ivan pusha git sam; testira Samsung Android + desktop
- **BUILD-GATE** svake isporuke: node --check ×5 + extract index `<script>`→check + css-tree 0 + html balans 0/0 + `node test-ui-v043.js` = **200/200** · `npm install css-tree jsdom --no-save` svaki turn · testovi samo rastu · verzija ×3 (server VERZIJA / sw CACHE_VERSION oi-vNNN / index #ja-verzija) · `// ⭐ vNNN` markeri se ne brišu

## 3. ŠTO JE NAPRAVLJENO v129→v183 (po sesijama)
**v136 (audit):** kompletan bug-hunt aplikacije → Batch 1 popravaka.
**v137–v153 (batchevi + sadržaj):** Batch 2+3 featurea; **novi modul dopis-pdf.js**; ingestion pravilničkih tablica iz GitHuba (mht/html/pdf); riješen slomljen ToUnicode PDF font (ć→d) CID remapiranjem; **copyright granica 🔒** — strani standardi samo kao originalni hrvatski konceptualni pregledi; merge-fix, ekran Novosti, popravci back-navigacije, popis gradiva.
**v154–v160:** Novosti highlight/clear; **pametna pretraga** (bez dijakritike, f_unaccent + trigram + word_similarity); pouzdan sistemski back; search-UX paket; Mentor deep-test; **pretplatnički tierovi** (free/pro/enterprise).
**v161–v175:** živi mjerač tokena; dashboard troškovna analitika + layout; sigurnosni popravak; **redizajn e-mailova**; tour fix; **multi-device sesije + GDPR izvoz**; sidebar bedževi; **INVESTITOR F1** (karta katastra, klik→čestica, obuhvat, kčbr, ručni m²); **Vještak privici do 50 MB** + progress + procjena troška; sadržaj: pravilnik otpornosti na požar puni JSON (46 čl. + 17 tablica); Investitor MASTER strategija (dokument).
**v176:** raščlamba potrošnje po značajkama u profilu + 🎁 **PROMO sustav** (tablica promo_akcije, rang-dodjela, hook na registraciju, admin panel). SHEMA.
**v177:** ⚙️ **tier_postavke** — DB override budžeta USD i mjesečnih kvota (pismeni/usmeni/vjestak/investitor; NULL=∞), keš 60 s, provedba u 4 toka, admin panel. SHEMA.
**v178:** GUŽVA FIX — fnSRetry (2× backoff 2–5 s na 529/429/overload/abort); klijent čuva djelomični odgovor + 🔄; trošak u done eventu ('⚡ Ovaj odgovor: N tokena', $ superadminu); propali pokušaj ne tereti budžet.
**v179:** INVESTITOR KARTA FIX — CSP je blokirao Leaflet/OSM/DGU (uzrok 'Karta se nije učitala') → dopuštene domene; Leaflet jsdelivr fallback; GPS Permissions API + invGpsUpute kartica; WFS default api.\*/cp/wfs, typeNames mala slova, bbox bez srs; probe +D_katalog.
**v180:** hotfix — `spavaj` globalan (v178 rušio fnSRetry); trajni test 197 (runtime bez injektiranih ovisnosti — **naučena lekcija**: mock u testu maskirao stvarni ReferenceError).
**v181:** Vještak drag&drop (#ai-dnd overlay, brojač enter/leave, tier gate) + Ctrl+V paste.
**v182:** ODREZAN ODGOVOR FIX — auto-nastavak do 3× na stop=max_tokens (prefill šav / [SUSTAV] s thinkingom); širi dokument-okidači; privitak→maxOdg≥4000.
**v183:** WMS podloga katastra prebačena s OSS (traži token → granice se nisu crtale!) na **ANONIMNI `api.uredjenazemlja.hr/services/inspire/cp_wms/wms`**, layers `cp:CadastralParcel`.

## 4. DGU SAGA — ZAKLJUČCI 🔒 (puna istina: OI-DGU-IZVORI-PODATAKA.md)
1. **DGU mailom potvrdio (2026-07-09):** CP WMS (`cp_wms/wms`), CP WFS (`cp/wfs`), **BU WFS (`bu/wfs` — zgrade iz DKP-a!)** = **ZA ANONIMNE KORISNIKE**, bez tokena. ATOM DKP: `oss.uredjenazemlja.hr/oss/public/atom/atom_feed.xml`.
2. ⚠ **api.uredjenazemlja.hr gateway odbija ne-browser klijente** (Claude fetcher: 400 na SVE, i na službene primjere; WFS zna vratiti i 500). Mjerodavan test = browser ili Railway (`/api/investitor/probe`, superadmin).
3. **OSS ATOM statika NE filtrira** (dokazano strojno 2026-07-09) → **ODLUKA: ATOM = primarni izvor geometrije (F1.5), WFS = fallback.**
4. Ivanov token (`DGU_TOKEN` u Railway ENV) = pričuva; ne objavljivati.
5. **Namjena/urbana pravila NISU kod DGU** → ZG Geoportal WMS `GUPZagreb_Public` + **GetFeatureInfo** (primarno za F3); `dservices8.arcgis.com/.../WFS_Geoportal_planirana_namjena` (2023); ISPU za nacionalno širenje; GeoHub NEMA GUP slojeve (provjereno). Bonus: ZG Ortofoto2022 WMS (besplatna podloga za Zagreb).
6. Jogunov ATOM katalog (adrese cijele HR s alternativeIdentifier!, upravne jedinice, DMR 84 GeoTIFF-a, zgrade, ceste, ELU…) — u OI-DGU dokumentu. ⚠ TTB skupovi su u EPSG:3035 (ne 3765).

## 5. AKTUALNO STANJE / PENDING
**Ivan (prije ili tijekom F1.5):** push v183 → hard-refresh → provjera granica na karti + čestica **2362 / Črnomerec** (ako radi = F1 KOMPLETAN; ako WFS i dalje 500 → svejedno idemo na ATOM, probe ispis meni informativno) · pushati OVE dokumente u docs/ · DGU_TOKEN u ENV (pričuva).
**Za F2 (kalkulator) Ivan priprema:** Excel Hercegovačke 56 + defaulti (gradnja €/m² nad/pod zemljom, projektiranje %, kamata, trajanje, np%, posr%, prosječan stan, neto/bruto, marža) + GUP Zagreb odredbe PDF.
**Claude sljedeća sesija = F1.5 ATOM** (recept: 10-INVESTITOR §V3.4; prvi prompt: PRVI-PROMPT-F15-ATOM.md).

## 6. PROTOKOLI (nepromjenjivo 🔒)
Hrvatski "ti", bez laskanja · kirurški str_replace, nikad full rewrite · 1 faza = 1 sesija · SHEMA → init-db · isporuka = ZIP 6 datoteka u /outputs + present_files · financijska/pravna aritmetika sveta · zlatni odgovori nikad klijentu · planEnforce samo server · Free 10 test-sesija/mj; Pro 19,99 € (sve + AI 50 por/dan + 3 usmene/dan).
